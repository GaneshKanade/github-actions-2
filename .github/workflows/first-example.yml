name: My first workflow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - uses: actions/checkout@v6
        - name: Welcome message
          run: echo "my first workflow in first job"

        - name: List files
          run: pwd
        
        - name: List files
          run: ls

        - name: Read files
          run: cat README.md
